<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".HealtyAppApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealtyApp"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".ui.citas.CrearCitaActivity"
            android:exported="false" />
        <activity
            android:name=".ui.citas.CitaDetalleActivity"
            android:exported="false" />
        <activity
            android:name=".ui.registros.CrearRegistroActivity"
            android:exported="false" />
        <activity
            android:name=".ui.registros.RegistrosActivity"
            android:exported="false" />
        <activity
            android:name=".ui.citas.CitasActivity"
            android:exported="false" />
        <activity
            android:name=".ui.patients.PatientsActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.HealtyApp" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>